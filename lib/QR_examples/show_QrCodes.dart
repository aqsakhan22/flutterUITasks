import 'package:flutter/material.dart';
class ShowQRCodes extends StatelessWidget {
  const ShowQRCodes({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("ShowQRCodes"),
      ),
      body: const Column(
        children: [

        ],
      ),
    );
  }
}

